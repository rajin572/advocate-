import ServicesPage from "@/components/Services/Services";
import React from "react";

export const metadata = {
  title: "Services",
};

const page = () => {
  return (
    <div>
      <ServicesPage />
    </div>
  );
};

export default page;
