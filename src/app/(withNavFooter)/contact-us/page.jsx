import ContactUs from "@/components/ContactUs/ContactUs";
import React from "react";

export const metadata = {
  title: "Contact Us",
};

const page = () => {
  return (
    <div>
      <ContactUs />
    </div>
  );
};

export default page;
