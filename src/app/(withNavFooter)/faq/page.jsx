import FAQ from "@/components/Home/FAQ";
import React from "react";

export const metadata = {
  title: "FAQ",
};

const page = () => {
  return (
    <div>
      <FAQ />
    </div>
  );
};

export default page;
